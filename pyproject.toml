[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "zotero-mcp-server"
dynamic = ["version"]
authors = [
  { name = "54yyyu", email = "54yyyu@github.com" },
]
description = "A Model Context Protocol server for Zotero"
keywords = ["zotero", "mcp", "model-context-protocol", "cli", "research", "citations", "bibliography"]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    # 1.13.5 retries HTTP-429 reads instead of returning the error body as
    # bytes and also fixes the filename passed through attachment_both().
    "pyzotero>=1.13.5",
    "mcp>=1.2.0",
    "python-dotenv>=1.0.0",
    "markitdown[pdf]",
    "pydantic>=2.0.0",
    "requests>=2.28.0",
    # FastMCP 4 changes the transport/runtime APIs used by this server.
    "fastmcp>=2.14.0,<4",
    # Imported directly by client.py and local_api.py.
    "httpx>=0.27",
    "unidecode>=1.3.0",
    "bibtexparser>=1.4,<2",
]

[project.optional-dependencies]
semantic = [
    # >=1.0.0 for chromadb.utils.embedding_functions.register_embedding_function,
    # used to register the custom OpenAI/Gemini/HF embedding functions (#315).
    "chromadb>=1.0.0",
    "sentence-transformers>=2.2.0",
    "openai>=1.30.0",
    "google-genai>=0.7.0",
    "tiktoken>=0.7.0",
]
pdf = [
    # >=1.24.2 for Page.cluster_drawings (vector figure detection in zotero_get_page_layout)
    "pymupdf>=1.24.2",
    "ebooklib>=0.18",
]
scite = [
    # Compatibility alias: Scite uses requests, which is already a core dependency.
]
all = [
    "zotero-mcp-server[semantic,pdf,scite]",
]
dev = [
    "zotero-mcp-server[all]",
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21",
    "pytest-timeout>=2.0.0",
    "ruff>=0.9.0",
]

[project.urls]
"Homepage" = "https://github.com/54yyyu/zotero-mcp"
"Bug Tracker" = "https://github.com/54yyyu/zotero-mcp/issues"
"Documentation" = "https://stevenyuyy.com/zotero-mcp/"
"Changelog" = "https://github.com/54yyyu/zotero-mcp/blob/main/CHANGELOG.md"

[project.scripts]
zotero-cli = "zotero_mcp.cli_standalone:main"
zotero-mcp = "zotero_mcp.cli:main"


[tool.hatch.version]
path = "src/zotero_mcp/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/zotero_mcp"]

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
timeout = 30
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]
